import { SubdirUrlPipe } from './subdir-url.pipe';

describe('SubdirUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SubdirUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
